# Tesseract training artifacts (reproducible via training scripts)
train/
docs/spikes/tessdata/

# Playwright browser profile (OPFS data, downloaded Chromium binary cache)
tools/.playwright-profile/

# Exported SQLite files (generated from OPFS via export-sqlite.ps1)
exported.sqlite
tools/*.sqlite

# Verification output (screenshots, summaries)
verify-output/

pet-source-pdf/
css-export/
catalogs.zip

# Phase 1 callout patch dataset (large, regenerable via ocr/dataset.py)
ocr/dataset/

__pycache__/
*.log
*credentials*
# Exported eval SQLite + eval output under groundtruth (the .json are tracked)
groundtruth/*/catalog.sqlite
groundtruth/*/eval.csv
